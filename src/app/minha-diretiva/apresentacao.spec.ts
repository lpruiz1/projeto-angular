import { Apresentacao } from './apresentacao';

describe('Apresentacao', () => {
  it('should create an instance', () => {
    const directive = new Apresentacao();
    expect(directive).toBeTruthy();
  });
});
